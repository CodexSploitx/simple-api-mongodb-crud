"use client";

import React from "react";

export default function STPMPanel() {
  return (
    <div className="bg-[var(--card)] border border-[var(--border)] rounded-lg p-6">
      <h2 className="text-lg font-semibold text-[var(--text)] mb-2">STPM</h2>
      <p className="text-sm text-[var(--text-muted)]">Coming soon. This section is ready to add options like a tabbed board.</p>
    </div>
  );
}

