"use client";



export default function STMPConfiguration() {
  return (
    <div>
      <div className="text-sm font-semibold text-[var(--text)] mb-2">Configuration</div>
      <div className="text-xs text-[var(--text-muted)]">No general settings yet. Use the OTP tab to manage OTP-related configuration.</div>
    </div>
  );
}
